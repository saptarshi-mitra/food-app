import { FilterGlutenPipe } from './filter-gluten.pipe';

describe('FilterGlutenPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGlutenPipe();
    expect(pipe).toBeTruthy();
  });
});
