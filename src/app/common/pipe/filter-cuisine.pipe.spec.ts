import { FilterCuisinePipe } from './filter-cuisine.pipe';

describe('FilterCuisinePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCuisinePipe();
    expect(pipe).toBeTruthy();
  });
});
