import { FilterDiaryProductsPipe } from './filter-diary-products.pipe';

describe('FilterDiaryProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDiaryProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
