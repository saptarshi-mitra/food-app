import { FilterVegPipe } from './filter-veg.pipe';

describe('FilterVegPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVegPipe();
    expect(pipe).toBeTruthy();
  });
});
